#page-heading
  #heading-left
    - if logged_in? 
      = render  'layouts/menu'
  #heading-right
    - if logged_in?
      = "Welcome #{user['name']}"
    = link_to 'Log Out', '#', :id => 'logout', :style => 'display:none;' 
    = link_to 'Log In', '#', :id => 'login', :style => 'display:none;'
